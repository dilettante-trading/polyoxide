# Authentication: use ONE of the two methods below (not both).

# Option 1: Builder API credentials (HMAC-signed)
BUILDER_API_KEY=
BUILDER_SECRET=
BUILDER_PASS_PHRASE=

# Option 2: Relayer API key (simpler alternative)
# See https://docs.polymarket.com/trading/gasless#using-relayer-api-keys
# RELAYER_API_KEY=
# RELAYER_API_KEY_ADDRESS=
